(()=>{const state={symbol:'AAPL',timer:null,bars:[],last:null};const $=id=>document.getElementById(id);const set=(id,v)=>{const e=$(id);if(e)e.textContent=v};const money=v=>v==null?'—':'$'+Number(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});function paint(q){if(!q)return;state.last=q;set('detail-price',money(q.price));set('detail-change',`${q.change>=0?'+':''}${q.change.toFixed(2)} (${q.pct>=0?'+':''}${q.pct.toFixed(2)}%)`);set('price-tag',Number(q.price).toFixed(2));const pill=$('realtime-pill');if(pill){pill.textContent='LIVE';pill.classList.add('live')}}function draw(){const g=$('candles');if(!g||!state.bars.length)return;const W=900,H=390,top=18,bottom=38,view=state.bars.slice(-72),lo=Math.min(...view.map(b=>b.l)),hi=Math.max(...view.map(b=>b.h)),range=Math.max(hi-lo,.01),xStep=W/Math.max(view.length,1);g.innerHTML='';const pts=[];view.forEach((b,i)=>{const x=i*xStep+xStep/2,y=v=>top+(hi-v)/range*(H-top-bottom),up=b.c>=b.o,cls=up?'chart-candle-up':'chart-candle-down',high=y(b.h),low=y(b.l),open=y(b.o),close=y(b.c);g.insertAdjacentHTML('beforeend',`<line x1="${x.toFixed(2)}" x2="${x.toFixed(2)}" y1="${high.toFixed(2)}" y2="${low.toFixed(2)}" class="${cls}" stroke-width="1"/><rect x="${(x-3).toFixed(2)}" y="${Math.min(open,close).toFixed(2)}" width="6" height="${Math.max(2,Math.abs(open-close)).toFixed(2)}" class="${cls}" rx="1"/>`);pts.push(`${x.toFixed(2)},${y(b.c).toFixed(2)}`)});const line=$('ma-line');if(line)line.setAttribute('points',pts.join(' '));const latest=view.at(-1),py=top+(hi-latest.c)/range*(H-top-bottom),pl=$('price-line');if(pl){pl.setAttribute('y1',py);pl.setAttribute('y2',py)}const area=$('chart-area');if(area){const d=`M ${pts[0]} ${view.slice(1).map((b,i)=>`L ${((i+1)*xStep+xStep/2).toFixed(2)} ${(top+(hi-b.c)/range*(H-top-bottom)).toFixed(2)}`).join(' ')} L ${W} ${H-bottom} L 0 ${H-bottom} Z`;area.setAttribute('d',d)}}async function refresh(){try{const r=await fetch(`/api/market/snapshot?symbols=${encodeURIComponent(state.symbol)}&crypto=BTC/USD,ETH/USD`,{cache:'no-store'});if(!r.ok)throw Error();const d=await r.json(),s=d.stocks?.[state.symbol];if(s){const p=Number(s.latestTrade?.p??s.dailyBar?.c),prev=Number(s.prevDailyBar?.c??p);paint({price:p,change:p-prev,pct:prev?((p/prev)-1)*100:0})}window.dispatchEvent(new CustomEvent('nexa:realtime',{detail:d}));const rb=await fetch(`/api/market/bars?symbol=${encodeURIComponent(state.symbol)}&timeframe=1Min&limit=72`,{cache:'no-store'});if(rb.ok){const bd=await rb.json();state.bars=(bd.bars||[]).map(b=>({t:b.t,o:+b.o,h:+b.h,l:+b.l,c:+b.c,v:+b.v}));draw();window.dispatchEvent(new CustomEvent('nexa:bar',{detail:state.bars}))}}catch{const pill=$('realtime-pill');if(pill){pill.textContent='RECONNECTING';pill.classList.remove('live')}}}async function start(){clearInterval(state.timer);await refresh();state.timer=setInterval(refresh,1000)}window.NexaRealtime={setSymbol:s=>{state.symbol=String(s||'AAPL').toUpperCase();state.bars=[];start()},start};window.addEventListener('nexa:symbol',e=>window.NexaRealtime.setSymbol(e.detail));start()})();